@page
@model IndexModel

@{
    ViewData["Title"] = "Index";
}

<div class="arriba">
<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Integracion[0].Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Integracion[0].Asignatura1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Integracion[0].Asignatura2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Integracion[0].Asignatura3)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Integracion) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Asignatura1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Asignatura2)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Asignatura3)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</div>